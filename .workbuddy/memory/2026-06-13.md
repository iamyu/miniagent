# 2026-06-13

## 安全修复 — API Key 泄漏
- 清空 `config.json`（项目级）和 `~/.miniagent/config.json`（用户级）中的明文 API Key，改为空字符串
- 创建 `D:\AI\miniagent\.env`，将 Key 写入：`DASHSCOPE_API_KEY=sk-...`
- `.env` 已由 `.gitignore` 排除，不会被提交
- 修改 `config.py`：添加 `load_dotenv()` 调用，自动加载 `.env` 文件
- 修改 `requirements.txt`：添加 `python-dotenv>=1.0.0` 依赖

## Bug 修复 — 角色名拼错
- `miniagent/chat.py` 第237行和第358行：`"assitant"` → `"assistant"`（少了一个 's'）
- 影响：修复前所有助手消息 role 字段错误，导致历史记录/LMM 上下文异常

## 优化 — Skill 触发器词边界匹配
- `miniagent/skills.py`：新增 `_trigger_matches()` 静态方法
- ASCII 触发器使用 `\b` 词边界匹配，防止 `"html"` 误匹配 `"html2canvas"`
- CJK 触发器使用子串匹配（中文无拼接风险）
- 修改 `match_triggers()` 调用新方法

## 重构 — 前端 formatMessageContent 函数拆分
- `miniagent/static/app.js`：原来 110 行单函数拆为 8 个独立函数的管线
- 新函数：`_extractFencedBlocks`, `_stripLlmHtml`, `_renderCodeBlocksToHtml`, `_renderInlineMarkdown`, `_renderFileLinks`, `_protectAnchors`, `_restorePlaceholders`, `_assembleParagraphs`
- 主函数 `formatMessageContent` 缩减到 ~30 行，清晰展示处理管线
- Node.js 语法检查通过

## 清理 — tools.py 未使用的 import
- 删除第8行 `import difflib`（全文件无任何 `difflib.` 调用）
- Python 导入语法检查通过

## 修复 — utils.py 日志格式统一为惰性 %s
- 7 处 logger 调用全部从 f-string 改为惰性 `%s` / `%d` / `%.Ns` 格式化
- f-string 在 logger 中会提前求值（即使日志级别被过滤），%s 只在需要时格式化
- 输入截断改用 `%.200s` / `%.500s` printf 精度语法，不再手动切片
- 测试验证格式化输出正确

## 优化 — WebSocket 流式渲染 rAF 限流
- `miniagent/static/app.js`：`updateAssistantBubble` 改为 `requestAnimationFrame` 限流
- 每 ~16ms 帧最多一次 `formatMessageContent` + `innerHTML`，批量合并同帧内的多个 text chunk
- 新增 `_scheduleBubbleRender()` 和 `_lastRenderedLen` 去重判断
- `appendAssistantBubble()` 创建新气泡时重置渲染状态
- Node.js 语法检查通过

## 修复 — shell-pptx SKILL.md Step 5 错误
- 根因：Step 5 只给了 CONFIG + slides 片段让 LLM "创建" build.js，LLM 猜错了 API 名（`{ createPPTX }` → `undefined`）和调用方式
- 修复：将 Step 5 从"创建 build.js"改为"复制模板 `scripts/build.js` + 修改 CONFIG 和 slides 两个区块"
- 模板已包含完整的 `findChromium()`、`findHtml2Pptx()`、`build()` 等 120 行逻辑，LLM 无需自己编写
- 与 Step 3（复制 html2pptx.js）和 Step 4（复制 HTML 模板）的工作流模式统一
