# 2026-06-28

## shell-pptx skill 硬编码路径修复

- 问题：SKILL.md 中 Step 3 / Step 5 的 PowerShell 示例直接写死 `$skillDir = "$env:USERPROFILE\.workbuddy\skills\shell-pptx"`，导致把 skill 拷贝到 `.claude` 或 `.lingma` 目录后执行复制命令会找不到文件。
- 修改：将两处示例改为环境自适应，先根据 `$skillsHome`（.workbuddy/.claude/.lingma 或项目目录）拼接 `$skillDir`，如路径不存在再回退到 `$PWD\skills\shell-pptx`。
- 二次修改：将 Step 3 / Step 5 改为使用 PowerShell 数组 + `Where-Object` 自动检测 `$PWD\skills\shell-pptx`、`~\.claude\skills\shell-pptx`、`~\.lingma\skills\shell-pptx`、`~\.workbuddy\skills\shell-pptx` 四个常见位置，并增加找不到时抛出异常的兜底。

## 用户级 vs 项目级 shell-pptx SKILL.md 对比

- 用户级 `~/.workbuddy/skills/shell-pptx/SKILL.md` 的 Step 3/5 中 Copy-Item 目标路径硬编码为 `"my-ppt\scripts\"` 和 `"my-ppt\build.js"`，而项目级 `D:/AI/miniagent/skills/shell-pptx/SKILL.md` 使用 `$projectName` 变量
- 项目级版本更好：一处修改全局生效，避免改名时遗漏导致路径错误
- Step 6 已同步修改为"必须自动执行"版本

## 积分商城划线价改造 PPT 生成

- 从 PRD `积分商城划线价改造PRD.md` 生成 Shell 品牌风格 5 页用户操作手册 PPT
- 页面结构：封面 → 改造概述(two-col) → 后台操作指南(three-cards) → 前端展示规则(two-col-compare) → 封底
- 项目目录：`D:/AI/miniagent/points-mall-ppt/`
- 输出：`积分商城划线价改造-用户操作手册.pptx` (123KB)
- 构建过程发现 pptxgenjs 缺失，自动安装后构建成功

## 积分商城划线价改造 15 页用户操作手册 PPT

- 基于 shell-pptx 技能「用户操作手册」内容模板生成完整 15 页 Shell 品牌 PPT
- 项目目录：`output/积分商城划线价改造-操作手册/`
- 页面结构：封面 → 目录 → 4 章节分隔 → 7 内容页 → FAQ → 封底
- 内容页类型：变化概览(two-col)、项目范围(two-col-compare)、前端展示说明(compare-row)、配置入口(two-col)、REQ-001/002/003(two-col)、FAQ(faq-grid)
- 构建问题：章节分隔页 `.divider-number` / `.divider-title` 使用 `<div>` 包裹文字导致 html2pptx 校验失败，改为 `<h1>`/`<h2>` 后通过
- 输出文件：`积分商城划线价改造-用户操作手册.pptx` (241KB)
- 依赖：pptxgenjs + playwright + sharp 均在项目 node_modules 中可用
